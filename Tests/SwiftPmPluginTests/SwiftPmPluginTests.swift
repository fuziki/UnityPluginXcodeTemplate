import XCTest
@testable import SwiftPmPlugin

final class SwiftPmPluginTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(SwiftPmPlugin().text, "Hello, World!")
    }
}
